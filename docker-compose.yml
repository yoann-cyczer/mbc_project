version: '3.9'

services:
  web:
    build: .
    container_name: mbc_dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    user: "1000:1000"

